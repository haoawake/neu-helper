# ─────────────────────────────────────────────────────────────
#  这个文件是防泄露的主防线。改之前想清楚。
#
#  两类东西绝对不能进仓库:
#    1. 凭据      —— Canvas token、邮箱授权码
#    2. 个人内容  —— 邮件正文、对话存档、课件、简报、我的课程和情况
#
#  凭据本来就在项目目录之外(~/.canvas-helper/),所以这里主要挡第二类。
# ─────────────────────────────────────────────────────────────

# ── 运行时的一切:邮件正文和附件、对话存档、课件(还有版权问题)、
#    简报、偏好(里面有"我的情况"那几条)、日志、备忘录、单实例锁
data/

# ── 这台机器上"我自己的东西"
#    CLAUDE.md 装着我的课程对照表和个人情况 —— 仓库里放的是
#    CLAUDE.example.md,第一次用复制一份再填
CLAUDE.md
.claude/settings.local.json

# ── 万一有人把凭据放错地方,这几条兜底
.canvas-helper/
config.json
mail.json
*.token
*_token.txt
.env

# ── Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 打包产物(Release 里发,不进仓库)
build/
dist/
*.spec.bak

# ── 编辑器 / 系统
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
